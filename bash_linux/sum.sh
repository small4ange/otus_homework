

if ! [[ -n "$1" && "$1" =~ ^-?[0-9]+(\.[0-9]+)?$ && -n "$2" && "$2" =~ ^-?[0-9]+(\.[0-9]+)?$ ]]; then
        echo "Оба аргумента должны быть заполнены и являться числами"
        exit 1
fi

result=$(awk -v n1="$1" -v n2="$2" 'BEGIN {print n1 + n2}')
echo "Результат = $result"
