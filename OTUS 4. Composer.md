# ООП в PHP
Тренды пришедшие из Java:
- все приложение состоит из классов, интерфейсов и трейтов
- классы лежат в файлах (один класс = один файл)
- связанные между собой классы - это пакеты (один пакет = один каталог)

Стажер Вася создает классы для игры
```php
class Paladin  
{  
    public string $name;  
    public int $hp;  
}  
  
class Druid  
{  
    public string $name;  
    public int $hp;  
}  
  
$leeroy = new Paladin();  
$leeroy->name = 'Лирой Дженкинс';  
$leeroy->hp = 100_000;  
  
echo "{$leeroy->name}, {$leeroy->hp}" . PHP_EOL;  
  
$ido = new Druid();  
$ido->name = 'Идо';  
$ido->hp = 150_000;  
  
echo "{$ido->name}, {$ido->hp}" . PHP_EOL;
```
Но для лучшей практики сохраняет классы Druid и Palladin в отдельные файлы, теперь их нужно как-то подключить.
### Подключение классов
```php
include 'Paladin.php';  
include 'Druid.php';
```
 Но если подключать таким образом возникает несколько проблем:
 - много включений в одном файле
 - пути могут поменяться
 - переиспользование одного класса и ошибки из-за этого
 - одинаковые названия классов

### Отличие require от include
- **`require` (Обязательный)**: Если файл отсутствует, PHP выдает **критическую ошибку (Fatal Error)**. Скрипт немедленно прекращает выполнение.
- **`include` (Желательный)**: Если файл отсутствует, PHP выдает **предупреждение (Warning)**. Скрипт продолжает работать дальше. 
### Автозагрузка
Простейший скрипт автозагрузки:
```
spl_autoload_reqister(
	static fn($class) => include "$class.php"
);
```
Регистрируем функцию и если php встречает класс, который ему неизвестен - просто вызываем include этого класса
Похожий более сложный механизм используется в фреймворках PHP
### Структурируем приложение

- DDD(Domain-Driven Design, или предметно-ориентированное проектирование) - когда вы описываете свое приложение и его сущности не языком программирования, а языком бизнеса.
#### Возможные проблемы:
##### Пространство имен (namespace)
```sh
mkdir -p ./src/Domain/Model ./src/Infrastructure/Http
```
```sh
mkdir -p ./src/Application/UseCase
```
Делаем два класса в разных папках с одинаковым названием:
```php
/**  
 * Запрос в техническую поддержку
 */
class Request  
{  
    public function getTitle(): string  
    {  
        return 'Запрос в техподдержку №4';  
    }  
}
```

```php
/**  
 * HTTP-запрос
 */
class Request  
{  
    public function getJsonBody(): string  
    {  
        return '{...}';  
    }  
}
```
Затем используем классы в другом файле:
```php
include 'src/Domain/Model/Request.php';  
include 'src/Infrastructure/Http/Request.php';
```

 Получаем ошибку - одновременное подключение одинаковых классов вызывает конфликт мол а какой мне класс использовать?

Для решения этой проблемы придумали **пространство имен (namespace)** - это что-то вроде имени каталога, в котором лежит файл. Пространство имен привязывают к имени приложения + структуре каталогов. 
![[Pasted image 20260730155001.png]]
![[Pasted image 20260730155051.png]]
![[Pasted image 20260730155225.png]]
- каждый класс лежит в своем каталоге
- пространства имен соответствуют каталогам
- у каждого класса получается уникальный адрес
- исчезает проблема одноименных классов

Возникает другая проблема:
![[Pasted image 20260730155420.png]]
![[Pasted image 20260730155430.png]]
Получается что-то типо автозамены.
Но опять появляется проблема:
![[Pasted image 20260730155533.png]]
Но это решается через as:
![[Pasted image 20260730155609.png]]
Это совсем немного нагружает время работы PHP, но PHP в целом не самый быстрый ЯП, особо не влияет
### Структура типичного класса
![[Pasted image 20260730155811.png]]
В современных IDE достаточно написать само тело class RequestController, все остальное IDE настраивает сама.
# Основы Composer

Современная разработка не заставляет придумывать веломипед и заново разрабатывать базовые классы для работы с бд и прочим. Для этого существуют различные библиотеки, которые необходимо в проект установить. 
Composer cам по себе необходим для управления библиотеками в проекте и установкой конкретных версий, так как для одних библиотек могут понадобится еще другие библиотеки, которые могут быть несовместимы с теми, которые нужны разработке проекта (это называется граф зависимостей и чаще всего его приходится разгребать руками, что занимает много времени и сил разработчика бедного)

Composer - пакетный менеджер, который работает так:
- мы подключаем Composer к нашему приложению
- говорим ему какие нам нужны библиотеки
- он сам разбирается со всеми нужными зависимостями и конфликтами версий
- после этого скачивает нужные пакеты
- и автоматически генерирует код для автозагрузки (как библиотек так и классов текущего проекта)
#### 1.  Инициализация приложения
```sh
composer init
```
- в названии (name) рекомендуется использовать префикс пространства имен (otus/test-app)
- тип (type) - project
- остальные поля можно не заполнять
![[Pasted image 20260730161555.png]]
После настройки подключения composer он генерирует composer.json
![[Pasted image 20260730161634.png]]
![[Pasted image 20260730162956.png]]
#### 2. Добавление пакетов
- имя пакета берем из документации к пакету
- composer сам выберет подходящую версию
- по умолчанию пакет устанавливается в каталог vendor
- после установки composer добавит классы пакета в автозагрузку и обновит composer.json, composer.lock

Например Slugify
```sh
composer require cocur/slugify
```
Теперь в composer.json внутри require находятся библиотеки установленные. В ней записано, что версия у нас не меньше 4.6
![[Pasted image 20260730163736.png]]
Также появляется файл composer.lock. В нем конкретно указана установленная версия
![[Pasted image 20260730163830.png]]
Благодаря composer.lock на компах других разработчиков будет установлена также 4.6 версия.
Также можем зайти в файл autoload.php
![[Pasted image 20260730163936.png]]
Здесь мы видим что composer использует spl_autoload_register()
Очистка autoload:
```sh
composer dump-autoload 
```

## Добавление пакетов, нужных только во время разработки
```sh
composer require --dev phpunit/phpunit
```
- эти пакеты будут установлены только локально
- во время деплоя на прод composer эти пакеты просто проигнорит
## Каталог vendor:
- создается автоматически
- в момент установки пакета Composer копирует в него исходный код пакета
- у каждого проекта отдельная копия этого каталога
- этот каталог принято добавлять в .gitignore тк composer может сам установить разработчику все необходимые пакеты, если разработчику передать composer.lock
## Файл composer.json:
- создается автоматически и дорабатывается вручную
- указывает желаемые версии установленных пакетов
- содержит настройки автозагрузки и необходимые расширения PHP
## Файл composer.lock:
- создается автоматически, лучше не редактировать
- содержит инфу о конкретных версиях установленных пакетов
- файл принято класть в git, чтобы у всех разработчиков был идентичный набор пакетов

## Шпаргалка по Composer
### Настройка автозагрузки
![[Pasted image 20260731154201.png]]
### Скачивание новой библиотеки в проект (composer require)

Команда composer require меняет composer.json и composer.lock сразу устанавливая нужную библиотеку, указанную после слова require.
Также скачивает файлы в папочку vendor и пересоздает автозагрузку классов.
Используется для установки пакетов из Packagist
### Установка всего проекта (composer install)
![[Pasted image 20260731154413.png]]
Делаем git pull, потом composer install
### Установка проекта на прод
![[Pasted image 20260731154518.png]]
Поставит на прод только те пакеты, которые не являются пакетами для разработки
(В Composer стоит либо require либо require-dev)

### Обновление пакетов до новых версий (composer update)
![[Pasted image 20260731155558.png]]
Лезет в composer.json и видит какие версии нужны, затем лезет в интернет и смотрит какие версии доступны. Если появились новые версии, которые совместимы с теми, что мы требуем в composer.json, то он скачивает обновление.
### Отличие install от update
![[Pasted image 20260731155849.png]]
### Версии пакетов
![[Pasted image 20260731155929.png]]
SEMVER - семантическое версионирование
Если изменилась первая цифра версии - проект нужно переделывать (как было с переходом с PHP 7.4 на PHP 8.1 в коммерческих проектах например)
### Как добавить пакет в приложение
![[Pasted image 20260731160650.png]]

## Типичные вопросы с собесов:
- чем отличается composer install от composer update?
composer install доустанавливает пакеты из жестко зафиксированных в  composer.lock, composer update переустанавливает пакеты из composer.json, обновляя их версии до последних и обновляя composer.lock
- чем отличается composer require от composer require --dev
composer require добавляет в composer.json в массив require необходимую библиотеку для прода, а --dev добавляет в массив require-dev необходимую библиотеку для разработки
- нужно ли хранить composer.lock в git?
нужно, чтобы другие разработчики могли установить себе те же версии зависимостей и они были у всех одинаковыми, а сам файл composer.lock обновляя composer update лучше обновлять одному разработчику, кто умеет в случае чего настраивать версии зависимостей

# Разработка пакетов

Есть смысл разработать свой собственный пакет, когда мы:
- хотим его переиспользовать в разных проектах
- пишем библиотеку для работы с публичным API

Разработаем пакет, который будет считать кол-во символов в строке
Создаем приложение
```sh
composer init
```
Записываем Package name 
В Package Type пишем library
>**library** означает переиспользуемый код для других программ, а **project** — готовое к работе приложение

Отказываемся от написания зависимостей 
![[Pasted image 20260731164148.png]]
.gitignore
```gitignore
# PhpStorm files
*.iml
.idea

# Project files
vendor
composer.lock
```
README.md
```markdown
# Процессор строк  
  
Краткое описание пакета  
  
## Требования  
  
- PHP 8.2  
  
## Установка  

composer require dmitry-kirillov/otus-composer-package

## Использование  

<?php  
  
$processor = new StringProcessor();  
echo $processor->getLength('my string')' // 9
```

Создаем на github публичный репозиторий, закидываем туда наш проект:
```sh
git init

git config user.email "dk@example.com" && git config user.name "DmitryKirillov"

git add .

git commit -m "Init commit"

git remote add origin git@github.com:DmitryKirillov/otus-composer-package.git

git push -u origin HEAD
```

## **Разработка самого проекта**

```php
class StringProcessor  
{  
    public function getLength(string $s): int  
    {  
        return strlen($s);  
    }  
}
```
И отправляем проект в гитхаб
```sh
git add .

git commit -m "Add StringProcessor"

git push origin HEAD
```
Затем каждое наше приложение должно быть с указанием версии, поэтому версию мы указываем как тег коммита в гите:
```sh
git tag -a 1.0.0 -m "Init version"
```

Можем локально посмотреть что входит в версию 1.0.0
```sh
git show 1.0.0
```

И отправляем тэг на удаленный репозиторий
```sh
git push origin 1.0.0
```
## Добавление в Packagist
Composer по умолчанию работает с сайтом Packagist. На этом сайте необходимо выложить наш пакет, чтобы он мог устанавливаться на других компьютерах через composer require.
![[Pasted image 20260803111711.png]]
![[Pasted image 20260803111724.png]]
![[Pasted image 20260803111754.png]]
Через Submit по инструкции добавляем наш пакет и теперь его могут скачать все через composer require
## Использование библиотеки
Устанавливаем библиотеку через composer require
```sh
composer require dmitry-kirillov/otus-composer-package
```
Применяем класс из библиотеки в коде
```php
$processor = new StringProcessor();  
echo $processor->getLength('my string');
```
## Внесение правок
>В PHP strlen() считает количество байт, занимаемых строкой, а не длину. Для поиска длины нужно использовать mb_strlen()

Вносим правки, заливаем проект:
```sh
git add .
```

```sh
git commit -m "Fix length calculation bug"
```

```sh
git push -u origin HEAD
```

Присваиваем новую версию. Если мы закоммитим изменения и не пропишем новый тэг - composer не заметит изменений и не загрузит их в библиотеку. Composer работает на основе тэгов.
```sh
git tag -a 1.0.1 -m "Bugfix"
```

```sh
git show 1.0.1
```

```sh
git tag
```

```sh
git push origin 1.0.1
```
Обновляем версию на Packagist. Если мы используем библиотеку в качестве ссылки на репозиторий гитхаба, то новая версия подтянется автоматически.
![[Pasted image 20260803114142.png]]
Смотрим какие впринципе актуальные версии у нас есть
```sh
composer show -l
```
![[Pasted image 20260803114249.png]]

Обновляем пакет
```
composer update dmitry-kirillov/otus-composer-package
```

>Если бы мы добавили в гит composer.lock, то у клиента была бы жесткая привязка к версиям других библиотек. То есть именно для библиотек лучше composer.lock не добавлять в git, чтобы клиентам не пришлось настраивать все остальные свои библиотеки под те, которые требуются вашей бмблиотеке.

Минимум README:
![[Pasted image 20260803114635.png]]
## Где публиковать свой пакет?
![[Pasted image 20260803114928.png]]
## Проверка знаний
![[Pasted image 20260803115958.png]]
 - приватный репозиторий нужен только для коммерческой разработки, чтобы код никто не менял и не переиспользовал для личных целей
 - major-версия пакета меняется, когда изменилась версия php, публичный апи пакета нашего - когда клиентам придется переделывать 